erst csv datei als test datai speichern (richtigen rows ausgeben, siehe2), dann die row beschriftung im txt dokument loeschen, mit denen er sons nich klar kommt, wenn man direkt aus csv zahlen auslesen will und plotten will, da die ueberschrift immer mit ausgelesen wird, aber string (wort) ist, kann nicht in float/zahl umgewandelt werden.

dann mit genfromtxt zahlen plotten.